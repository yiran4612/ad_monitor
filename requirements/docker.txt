# ============================================================
# 已废弃：容器镜像改用根目录 requirements.txt（单文件、无 psycopg2）。
# 本文件仅保留以兼容旧引用，请勿再新增依赖到此文件。
# Install:  pip install -r requirements/docker.txt
# ============================================================
-r requirements.txt
