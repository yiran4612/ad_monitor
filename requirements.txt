# ============================================================
# ad_monitor - 容器镜像依赖（Dockerfile 使用，单文件）
# Install:  pip install -r requirements.txt
# Python:   python:3.11-slim
#
# 与 requirements/base.txt 的区别：
#   - 版本对齐本地 .venv 实际安装（Django 5.2.17 / DRF 3.17 / Celery 5.6）
#   - 不含 mysqlclient（slim 镜像无编译链，且容器场景使用 SQLite）
#   - 不含 psycopg2-binary（暂不引入 Postgres，后续切换时再追加）
# ============================================================

# --- Core Framework ---
Django>=5.2,<6.0
djangorestframework>=3.15,<3.18
django-cors-headers>=4.0,<5.0
django-filter>=23.0

# --- Authentication ---
djangorestframework-simplejwt>=5.3

# --- Storage & Cloud (Alibaba OSS) ---
django-storages>=1.14
oss2>=2.18

# --- Third-Party Integrations ---
# 注：wechatpy 2.0 仅提供 alpha 版本，Python 3.11 下无稳定版，固定 1.8.x
wechatpy>=1.8.18,<2.0
alibabacloud_dysmsapi20170525

# --- Async Tasks (Celery + Redis) ---
celery>=5.3,<6.0
redis>=5.0
django-celery-results>=2.5

# --- Media Processing ---
Pillow>=10.0

# --- Environment & Config ---
python-dotenv>=1.0

# --- Developer Tools ---
django-extensions>=3.2

# --- API Documentation ---
drf-spectacular>=0.27

# --- Import / Export ---
django-import-export>=4.0

# --- File Lifecycle ---
django-cleanup>=8.0

# --- Production Server ---
gunicorn>=21.2
